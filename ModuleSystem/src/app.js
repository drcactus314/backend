const culcArea =(width, length)=> width * length;

const calcPerimetr = (length, width) => (length + width) * 2;

const isSquare = (length, width)=> length === width;

export{culcArea, calcPerimetr, isSquare};